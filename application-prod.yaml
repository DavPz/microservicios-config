spring:
  application:
    name: Config-Prod
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-url: true
    service-url:
      defaultZone: http://localhost:8761/eureka
